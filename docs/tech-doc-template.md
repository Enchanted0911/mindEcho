# 【设计文档】{项目名称}技术方案

> **约束**: 所有章节必须填写，不需要的项目填「无」，但必须考虑。

---

## 0. 基本原则

<!-- 必须包含项目遵循的架构原则和非功能性要求；不能留空，不需要的填「无」 -->

- 分层架构原则：{API层 → 用户接口层 → 应用层 → 领域层 → 基础设施层}
- Region 数据隔离原则：{所有核心业务数据按 region 隔离}
- 幂等性原则：{MQ 消费、DB 操作均需实现幂等}
- 测试优先原则：{先写测试用例，确保失败后再实现功能}
- 其他：{补充项目特有的架构原则}

---

## 1. 背景

### 1.1 业务价值

{为什么要做这个需求，解决什么业务痛点}

### 1.2 要解决的问题

{具体描述当前存在的问题或机会}

### 1.3 PRD / 需求文档

| 文档 | 链接 |
|------|------|
| PRD | {PRD 链接} |
| 其他 | {补充文档链接} |

---

## 2. 需求前置评估

### 2.1 业务 & 产品关注点

| 功能点 | 评估项 | 本次需求 | 备注 |
|--------|--------|----------|------|
| 功能地区差异化控制 | by region/cityId 是否有差异 | {填写} | {填写} |
| 场景 | 外卖、打赏、券包等 | {填写} | {填写} |
| 配送方式 | 配送、自取 | {填写} | {填写} |
| 支付 | 普通支付、钱包支付等 | {填写} | {填写} |
| 是否涉及资金 | 如涉及，需引入结算评估 | {填写} | {填写} |
| 敏感合规治理 | 手机号、邮箱、地址等敏感信息 | {填写} | {填写} |

### 2.2 技术关注点

| 评估项 | 是否涉及 | 说明 & 要求 |
|--------|----------|-------------|
| 多机房配置 | {是/否} | {非必要不引入差异；如有差异，说明原因} |
| 终端版本控制 | {是/否} | {后端协议 vs 前端版本的兼容策略} |
| 接口变更 | {是/否} | {新增/修改的下游接口依赖} |
| 存储变更 | {是/否} | {MySQL、缓存、MQ 等存储变更} |
| 「三高」保证 | {是/否} | {并发安全、性能指标、一致性要求} |
| 系统能力 | {是/否} | {对系统能力的特殊要求} |

---

## 3. 整体架构设计

### 3.1 系统交互图

<!-- 必须包含系统级交互图（C4 Context 或系统交互图），使用 Mermaid 绘制 -->
<!-- 画图前必须先调用 mermaid_chart_generator skill -->

```mermaid
graph TB
    {填入架构图}
```

### 3.2 涉及系统及职责

| 系统 | 职责 | 通信方式 |
|------|------|----------|
| {系统A} | {职责描述} | {RPC / MQ / HTTP} |
| {系统B} | {职责描述} | {RPC / MQ / HTTP} |

> 🚫 **禁止系统间互相调用**（禁止 A 调 B 且 B 调 A 的双向 RPC/HTTP/Thrift）。
> ✅ 允许单向 RPC；若需双向交互，采用单向 RPC + MQ（如 A→B 用 RPC，B→A 用 MQ）。

### 3.3 技术选型

| 技术点 | 选型 | 理由 |
|--------|------|------|
| {分层架构} | {DDD 四层架构} | {理由} |
| {数据库} | {MySQL} | {理由} |
| {缓存} | {Redis} | {理由} |
| {消息队列} | {Kafka / RocketMQ} | {理由} |

---

## 4. 详细方案

### 4.1 干系方表

| 角色 | 范围 | 干系人 | 影响流程 | SOP 建议 | 备注 |
|------|------|--------|----------|----------|------|
| 业务人员 | {业务链条} | {姓名/团队} | {影响描述} | {需要/不需要} | {备注} |
| 产品、运营 | {上下游产品} | {姓名/团队} | {影响描述} | {需要/不需要} | {备注} |
| 上游服务研发 | {依赖的上游系统} | {姓名/团队} | {影响描述} | {需要/不需要} | {备注} |
| 下游依赖研发 | {调用本系统的下游} | {姓名/团队} | {影响描述} | {需要/不需要} | {备注} |
| 数据组 | {表结构变更} | {姓名/团队} | {影响描述} | {需要/不需要} | {备注} |
| 开放平台 | {对外接口变更} | {姓名/团队} | {影响描述} | {需要/不需要} | {备注} |

### 4.2 业务流程图

<!-- 至少包含一个流程图和一个时序图 -->
<!-- 画图前必须先调用 mermaid_chart_generator skill -->

#### 4.2.1 流程图

```mermaid
graph TD
    {填入流程图}
```

#### 4.2.2 时序图

<!-- 时序图参与者至少包含：User / Controller / Service / Domain / Repository / DB(或MQ) -->

```mermaid
sequenceDiagram
    {填入时序图}
```

### 4.3 核心数据模型

#### 4.3.1 ER 图

<!-- 画图前必须先调用 mermaid_chart_generator skill -->
<!-- 新增表/字段用 %%[NEW] 标注，修改的字段用 %%[MODIFIED] 标注 -->

```mermaid
erDiagram
    {填入 ER 图}
```

#### 4.3.2 表结构设计

<!-- 所有表必须包含：id, region, create_by, update_by, ctime, utime, is_valid -->
<!-- 主键：bigint NOT NULL AUTO_INCREMENT；时间字段：bigint 存时间戳 -->
<!-- 变更字段前缀标记：🆕 新增 / ✏️ 修改 / 🗑️ 删除 -->

##### {表名1}

| 字段 | 类型 | 约束 | 默认值 | 说明 | 变更标记 |
|------|------|------|--------|------|----------|
| id | bigint | NOT NULL AUTO_INCREMENT | - | 主键 | - |
| region | varchar(32) | NOT NULL | - | 地区 | - |
| {字段} | {类型} | {约束} | {默认值} | {说明} | {🆕/✏️/🗑️/-} |
| create_by | varchar(64) | NOT NULL | '' | 创建人 | - |
| update_by | varchar(64) | NOT NULL | '' | 更新人 | - |
| ctime | bigint | NOT NULL | 0 | 创建时间 | - |
| utime | bigint | NOT NULL | 0 | 更新时间 | - |
| is_valid | tinyint | NOT NULL | 1 | 软删除标记 | - |

<!-- 如有多张表，继续添加 -->

#### 4.3.3 索引设计

| 表名 | 索引名 | 字段 | 类型 | 说明 |
|------|--------|------|------|------|
| {表名} | {idx_xxx} | {字段列表} | {UNIQUE / NORMAL} | {说明} |

### 4.4 接口定义列表

#### 4.4.1 接口清单

| 序号 | 接口类型 | 接口名称 | 说明 | 变更标记 | SC 链接 |
|------|----------|----------|------|----------|---------|
| 1 | RPC | {方法名} | {说明} | {🆕/✏️/🗑️/-} | {SC链接} |
| 2 | HTTP | {URL} | {说明} | {🆕/✏️/🗑️/-} | {SC链接} |

#### 4.4.2 接口详细定义

<!-- 返回值使用 BaseResultDTO<T> 或 BasePageResultDTO<T> -->
<!-- 参数超过 3 个必须使用 Request 对象封装 -->

##### {接口名称}

- **类型**: {RPC / HTTP}
- **服务/路径**: {服务名 / URL}
- **方法**: {方法签名}
- **请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| {参数名} | {类型} | {是/否} | {说明} |

- **返回值**: `BaseResultDTO<{类型}>`
- **错误码**:

| 错误码 | 说明 |
|--------|------|
| {code} | {说明} |

#### 4.4.3 MQ 消息定义

| Topic | Tag | 消息体 | 幂等键 | 重试策略 | 顺序性 |
|-------|-----|--------|--------|----------|--------|
| {topic} | {tag} | {消息结构} | {幂等键字段} | {重试次数} | {是否需要顺序} |

#### 4.4.4 定时任务

| 任务名 | Cron | 说明 |
|--------|------|------|
| {任务名} | {表达式} | {说明} |

#### 4.4.5 API artifact 版本

<!-- 若本次涉及对外 API 契约变更，必须填写此节 -->

| 项目 | 内容 |
|------|------|
| groupId:artifactId | {填写} |
| 当前版本 | {填写} |
| 目标版本 | {*-SNAPSHOT} |
| 需修改的 pom.xml | {路径} |
| 下游依赖升级说明 | {填写} |
| 兼容与回滚说明 | {填写} |

### 4.5 i18n 国际化

| 项目 | 说明 |
|------|------|
| 多语言 | {涉及的多语言内容} |
| 多时区 | {时区相关处理} |
| 多币种 | {币种相关处理} |

### 4.6 非功能性设计

#### 4.6.1 技术安全变更六要两不要

{填写技术安全变更评估}

#### 4.6.2 兼容性设计

{新老版本兼容策略、前后端协议兼容方案}

#### 4.6.4 容量评估

| 资源 | 当前容量 | 预估增量 | 风险 | 应对方案 |
|------|----------|----------|------|----------|
| DB | {当前} | {增量} | {风险} | {方案} |
| 缓存 | {当前} | {增量} | {风险} | {方案} |
| MQ | {当前} | {增量} | {风险} | {方案} |
| QPS | {当前} | {增量} | {风险} | {方案} |

#### 4.6.5 监控 & 埋点 & 验证设计

| 监控指标 | 类型 | 阈值 | 告警方式 |
|----------|------|------|----------|
| {指标名} | {业务/技术} | {阈值} | {告警渠道} |

#### 4.6.6 灰度 & 降级预案

- **灰度策略**: {按 region/比例/用户 灰度的具体流程和时间点}
- **降级开关**: {开关配置 key，默认值}
- **降级行为**: {降级后的默认行为}

#### 4.6.7 安全性设计

- **鉴权方案**: {接口鉴权方式}
- **幂等设计**: {幂等键设计，防重机制}
- **数据加密**: {敏感数据加密方案}
- **限流防刷**: {限流策略}

#### 4.6.8 资金安全监控

<!-- 涉及资金时必填，不涉及填「无」 -->

| 监控项 | 规则 | 告警 |
|--------|------|------|
| 资金流水 | {流水记录方式} | {异常告警规则} |
| 对账机制 | {对账周期和方式} | {差异告警规则} |
| 风控规则 | {风控拦截规则} | {触发告警规则} |

#### 4.6.9 多机房配置及部署

<!-- 涉及多机房时必填，不涉及填「无」 -->

| 机房 | 部署说明 | 配置差异 |
|------|----------|----------|
| {机房名} | {部署架构} | {差异说明} |

---

## 5. 自测 case 覆盖、测试建议

### 5.1 自测 case 覆盖

| 模块 | 测试范围 | 覆盖率目标 |
|------|----------|------------|
| DAO | {DAO 方法覆盖} | ≥80% |
| Repository | {Repository 方法覆盖} | ≥80% |
| DomainService | {领域服务核心逻辑} | ≥80% |
| AppService | {应用层用例编排} | ≥80% |

### 5.2 测试建议

<!-- 给 QA 的测试建议，包括功能测试、性能测试、异常测试 -->

| 测试类型 | 测试场景 | 预期结果 |
|----------|----------|----------|
| 功能测试 | {场景} | {预期} |
| 性能测试 | {场景及指标，如 100w 数据 < 2h} | {预期} |
| 异常测试 | {异常场景} | {预期} |

---

## 6. 发布方案

### 6.1 灰度策略

| 阶段 | 灰度范围 | 观察时间 | 观察指标 | 通过条件 |
|------|----------|----------|----------|----------|
| 1 | {10% / 单region} | {N小时} | {指标} | {条件} |
| 2 | {50%} | {N小时} | {指标} | {条件} |
| 3 | {100%} | - | {指标} | {条件} |

### 6.2 回滚方案

| 异常场景 | 回滚操作 | 责任人 |
|----------|----------|--------|
| 代码异常 | {代码回滚方式} | {姓名} |
| 数据异常 | {数据修复/回滚方式} | {姓名} |
| 配置异常 | {配置回滚方式} | {姓名} |

### 6.3 监控及验证方案

| 验证项 | 验证方式 | 验证人 | 时间窗口 |
|--------|----------|--------|----------|
| 功能验证 | {验证步骤} | {姓名} | {时间} |
| 数据验证 | {数据校验方式} | {姓名} | {时间} |
| 监控指标 | {监控 dashboard} | {姓名} | {时间} |

---

## 7. 排期

| 阶段 | 内容 | 开始 | 结束 | 负责人 | 依赖 |
|------|------|------|------|--------|------|
| 开发 | {模块} | {日期} | {日期} | {姓名} | {依赖} |
| 联调 | {联调内容} | {日期} | {日期} | {姓名} | {依赖} |
| 测试 | {测试内容} | {日期} | {日期} | {姓名} | {依赖} |
| 灰度 | {灰度阶段} | {日期} | {日期} | {姓名} | {依赖} |
| 全量 | {全量发布} | {日期} | {日期} | {姓名} | {依赖} |

